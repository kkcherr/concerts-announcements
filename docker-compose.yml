services:
  concert-bot:
    build: .
    container_name: concert-digest-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml
